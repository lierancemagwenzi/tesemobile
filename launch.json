{
    // Use the standard Flutter debug configuration as a base
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flutter (Release Mode)",
            "request": "launch",
            "type": "dart",
            // Key: use the "--release" flag
            "args": [
                "--release"
            ]
        },
        {
            "name": "Flutter",
            "request": "launch",
            "type": "dart"
        }
    ]
}